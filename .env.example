# Base URL of the rebalancer-api backend (see ../backend/README.md's
# "Running the API" section). Only used by the sub-portfolios feature
# (/app/portfolios) - the legacy single-vault /app dashboard reads the
# contract directly and needs no backend at all.
NEXT_PUBLIC_API_URL=http://localhost:8080
